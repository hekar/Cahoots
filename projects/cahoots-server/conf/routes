# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                          			controllers.Portal.index

POST     /app/login                          controllers.Application.authenticate
POST     /app/logout                         controllers.Application.deauthenticate


POST     /collaborator/createGroup  			controllers.collaborator.Group.createGroup

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               		controllers.Assets.at(path="/public", file)
